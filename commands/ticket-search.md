---
description: Search Jira tickets using JQL
allowed-tools: Bash(aide:*)
---

Search for Jira tickets matching a query.

Run the following command:

```bash
aide jira search "$ARGUMENTS"
```

Display results as a formatted list showing ticket key, summary, status, and assignee.
